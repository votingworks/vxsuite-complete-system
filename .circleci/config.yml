version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: No-op
          command: echo "No tests configured yet"

workflows:
  main:
    jobs:
      - build
